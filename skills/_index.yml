generated: '2026-07-27'
method: generated
source: openapi/eia-api-v2-openapi.yml, https://www.eia.gov/opendata/documentation.php,
  https://www.eia.gov/opendata/bulk/manifest.txt
note: |
  EIA publishes no AGENTS.md or provider-authored agent skills. These are generated by the
  API Evangelist pipeline and grounded in verified paths from the published OpenAPI - the spec
  carries operationIds on only 6 of 278 operations, so every step is addressed by method + path
  and every path below was grepped out of openapi/eia-api-v2-openapi.yml. Nothing is invented;
  facet ids and column names are deliberately left as placeholders the agent must resolve from
  the API's own metadata routes.
skills:
- file: eia-discover-and-query-a-dataset.md
  name: Discover and query an EIA dataset
  description: The universal APIv2 pattern - walk the route tree, read the leaf contract,
    resolve facet values, then query with data[], facets[], frequency, date bounds, sort and
    offset/length paging.
  api: openapi/eia-api-v2-openapi.yml
  operations:
  - GET /v2
  - GET /v2/electricity
  - GET /v2/electricity/retail-sales
  - GET /v2/electricity/retail-sales/facet
  - GET /v2/electricity/retail-sales/facet/{facet_id}
  - GET /v2/electricity/retail-sales/data
  - POST /v2/electricity/retail-sales/data
- file: eia-hourly-grid-operating-data.md
  name: Pull hourly grid operating data from EIA
  description: The /electricity/rto family - hourly and daily balancing-authority demand,
    demand forecast, net generation, interchange and generation by fuel type (Form EIA-930).
  api: openapi/eia-api-v2-openapi.yml
  operations:
  - GET /v2/electricity/rto
  - GET /v2/electricity/rto/region-data
  - GET /v2/electricity/rto/region-data/facet/{facet_id}
  - GET /v2/electricity/rto/region-data/data
  - POST /v2/electricity/rto/region-data/data
  - GET /v2/electricity/rto/fuel-type-data/data
  - GET /v2/electricity/rto/interchange-data/data
  - GET /v2/electricity/rto/region-sub-ba-data/data
  - GET /v2/electricity/rto/daily-region-data/data
  - GET /v2/electricity/rto/daily-fuel-type-data/data
  - GET /v2/electricity/rto/daily-interchange-data/data
  - GET /v2/electricity/rto/daily-region-sub-ba-data/data
- file: eia-bulk-download.md
  name: Bulk-download EIA datasets without an API key
  description: Read the bulk manifest (29 datasets, verified 2026-07-27) and stream whole
    datasets as zipped newline-delimited JSON - the correct alternative to scraping APIv2, and
    the only EIA surface needing no key.
  api: https://www.eia.gov/opendata/bulk/manifest.txt
  operations:
  - GET /opendata/bulk/manifest.txt
  - GET /opendata/bulk/{dataset}.zip
